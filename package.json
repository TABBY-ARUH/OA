{
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=7.0.0"
  },
  "name": "openarb_frontend",
  "scripts": {
    "build": "echo 'skip build'",
    "prebuild": "echo 'skip prebuild'",
    "pretest": "echo 'skip pretest'",
    "start": "echo 'skip start'",
    "test": "echo 'skip test'"
  },
  "type": "module",
  "workspaces": [
    "src/openarb_frontend_frontend"
  ]
}